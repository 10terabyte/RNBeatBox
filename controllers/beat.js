import firestore from '@react-native-firebase/firestore';
import database from '@react-native-firebase/database';
export const handleFollow = (beatID, userID) => {
    return new Promise(function (resolve, reject) {
    });
}